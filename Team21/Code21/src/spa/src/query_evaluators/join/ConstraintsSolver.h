/*#pragma once

//Assume for ClauseResult
#include "query/Clause/ClauseResult.h"
#include "query/query.h"

namespace qps {
	class ConstraintsSolver {
	private:
		static ClauseResult suchThatAssignPatternClauseResult(const Query& query);

	public:
		static ClauseResult solve(const Query& query);
	};
}*/
