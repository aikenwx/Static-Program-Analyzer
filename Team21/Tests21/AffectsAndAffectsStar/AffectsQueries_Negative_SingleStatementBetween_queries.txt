1 - s1, s2 has exactly one assign statement between which modifies v

Select BOOLEAN such that Affects(1, 3)
FALSE
5000
2 - s1, s2 has exactly one read statement between which modifies v

Select BOOLEAN such that Affects(4, 6)
FALSE
5000
3 - s1, s2 has exactly one call statement between which modifies v in a top-level non-container statement

Select BOOLEAN such that Affects(7, 9)
FALSE
5000
4 - s1, s2 has exactly one call statement between which modifies v in a top-level container statement

Select BOOLEAN such that Affects(10, 12)
FALSE
5000
5 - s1, s2 has exactly one call statement between which modifies v in a 2+-level non-container statement

Select BOOLEAN such that Affects(13, 15)
FALSE
5000
